# function with outputs
def format_name(f_name, l_name):
    formatted_first = f_name.title()
    formatted_last = l_name.title()

    return f"{formatted_first} {formatted_last}"


format_name("ViCENte", "CEJA")

